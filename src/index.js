import React from 'react';
import ReactDOM from 'react-dom/client';
import { ComprasMovilesApp } from './ComprasMovilesApp';

import 'antd/dist/antd.css';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <ComprasMovilesApp />,
);
